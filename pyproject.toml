[project]
name = "qry-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg2-binary", 
    "google-cloud",
    "google-auth",
    "google-auth-oauthlib", 
    "google-auth-httplib2",
    "faker"
]
